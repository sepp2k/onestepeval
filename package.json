{
    "name": "onestepeval",
    "private": true,
    "scripts": {
        "build": "tsc -p .",
        "lint": "eslint src && prettier --check src",
        "cli": "npm run build && node --enable-source-maps out/cli.js"
    },
    "devDependencies": {
        "@typescript-eslint/eslint-plugin": "^5.59.6",
        "@typescript-eslint/parser": "^5.59.6",
        "eslint": "^8.40.0",
        "prettier": "^2.8.8",
        "typescript": "^5.0.4"
    }
}
